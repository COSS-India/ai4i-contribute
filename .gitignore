# =============================================================================
# IMPORTANT SECURITY NOTICE:
# DO NOT COMMIT .env FILES TO GIT!
# .env files contain sensitive configuration data (API keys, secrets, etc.)
# and should NEVER be pushed to version control.
# Only commit .env.example files as templates for other developers.
# =============================================================================

# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/
.last_build_id
pubspec.lock
android/app/src/main/res/build/ios/

# Web related

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

.vscode/


# "JSON to Dart Model" generator tracker files locations.
/.json_models/
models.jsonc
http_services/ios/Flutter/Generated.xcconfig
http_services/ios/Flutter/flutter_export_environment.sh
http_services/ios/Runner/GeneratedPluginRegistrant.h
http_services/ios/Runner/GeneratedPluginRegistrant.m
http_services/android/local.properties
ios/build/ios/XCBuildData/PIFCache/

# Local files
*.jks

# IMPORTANT: Do NOT include .env files when pushing to git!
# .env files contain sensitive configuration data and should not be committed to version control.
# .env.example files are also ignored to prevent accidental commits of example configurations.
# Keep .env files for configuration but exclude sensitive local overrides
# .env files are now tracked for configuration but sensitive data should be in .env.local files

# Backend-specific ignores
# Database files
*.db
*.sqlite
*.sqlite3

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/

